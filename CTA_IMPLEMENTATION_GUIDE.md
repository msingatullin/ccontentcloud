
## ✅ **ЧТО ИЗМЕНИЛОСЬ**

`call_to_action` теперь **массив строк** (`List[str]`), а не одна строка.

---

## 🎯 **КАК ОТПРАВЛЯТЬ С ФРОНТЕНДА**

### **Формат данных:**

```javascript
call_to_action: [
  "Подпишись на Telegram канал",      // чекбокс 1 + текст
  "https://t.me/yourchannel",         // поле URL
  "Переходи на сайт",                 // чекбокс 2 + текст
  "https://example.com?utm=post123",  // поле URL + UTM
  "Получи скидку 20%"                 // чекбокс 3 + текст
]
```

### **Правила:**

1. **Каждый чекбокс + его поле = отдельный элемент массива**
2. **Можно чередовать:** текст, ссылка, текст, ссылка
3. **Максимум 10 элементов**
4. **Можно пустой массив:** `[]` (AI сгенерирует дефолтный CTA)

---

## 🤖 **КАК AI ОБРАБАТЫВАЕТ**

### **Автоматическое разделение:**

AI разделяет элементы на:
- **Тексты** - всё что не начинается с `http://` или `https://`
- **Ссылки** - всё что начинается с `http://` или `https://`

### **Адаптация под платформы:**

#### **Telegram:**
```
Подпишись на Telegram канал
https://t.me/yourchannel
Переходи на сайт
https://example.com
```
Использует **все элементы**, каждый с новой строки.

#### **Instagram:**
```
Подпишись на Telegram канал 🔗 Ссылка в bio
```
Использует **только первый текст** + "Ссылка в bio" (Instagram не поддерживает ссылки в постах).

#### **VK:**
```
Подпишись на Telegram канал
https://t.me/yourchannel
```
Использует **первый текст + первая ссылка**.

---

## 📝 **ПРИМЕРЫ ИЗ UI**

### **Пример 1: Простой CTA**

**UI:**
```
☑ Подписка на канал
  Текст: "Подпишись на наш канал"
  URL: "https://t.me/channel"
```

**Отправляем:**
```javascript
call_to_action: [
  "Подпишись на наш канал",
  "https://t.me/channel"
]
```

---

### **Пример 2: Несколько действий**

**UI:**
```
☑ Telegram
  Текст: "Подпишись на Telegram"
  URL: "https://t.me/channel"

☑ Сайт
  Текст: "Переходи на сайт"
  URL: "https://example.com"

☑ Скидка
  Текст: "Получи скидку 20%"
```

**Отправляем:**
```javascript
call_to_action: [
  "Подпишись на Telegram",
  "https://t.me/channel",
  "Переходи на сайт",
  "https://example.com",
  "Получи скидку 20%"
]
```

---

### **Пример 3: Только текст (без ссылок)**

**UI:**
```
☑ Вовлечение
  Текст: "Что думаешь? Пиши в комментариях!"
```

**Отправляем:**
```javascript
call_to_action: [
  "Что думаешь? Пиши в комментариях!"
]
```

---

### **Пример 4: Пустой массив**

**UI:**
```
(Ничего не выбрано)
```

**Отправляем:**
```javascript
call_to_action: []
```

**AI сгенерирует дефолтный CTA:**
- Telegram: "Подписывайтесь на наш канал!"
- VK: "Что думаете? Пишите в комментариях!"
- Instagram: "Понравилось? Сохраните пост!"

---

## 🔧 **РЕАЛИЗАЦИЯ НА ФРОНТЕ**

### **React пример:**

```jsx
const [ctaItems, setCtaItems] = useState([]);

// Добавить элемент
const addCTA = (text, url = null) => {
  const newItems = [...ctaItems];
  newItems.push(text);
  if (url) {
    newItems.push(url);
  }
  setCtaItems(newItems);
};

// Удалить элемент
const removeCTA = (index) => {
  const newItems = ctaItems.filter((_, i) => i !== index);
  setCtaItems(newItems);
};

// Отправить
const submitContent = async () => {
  const response = await fetch('/content/create', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      ...otherFields,
      call_to_action: ctaItems  // Массив
    })
  });
};
```

---

## ⚠️ **ВАЖНО**

### **❌ НЕ ДЕЛАЙ ТАК:**

```javascript
// НЕ склеивай в строку!
call_to_action: "Подпишись. https://t.me/channel. Переходи на сайт"

// НЕ отправляй как строку!
call_to_action: ctaItems.join(', ')
```

### **✅ ДЕЛАЙ ТАК:**

```javascript
// Отправляй массив как есть
call_to_action: ctaItems

// Или явно массив
call_to_action: [
  "Текст 1",
  "https://link1.com",
  "Текст 2"
]
```

---

## 📊 **ВАЛИДАЦИЯ**

Бэкенд проверяет:
- ✅ Тип: `List[str]` (массив строк)
- ✅ Максимум: 10 элементов
- ✅ Каждый элемент: строка

**Ошибки:**
- Если отправишь строку → `ValidationError`
- Если больше 10 элементов → `ValidationError`
- Если элемент не строка → `ValidationError`

---

## 🎨 **UI/UX РЕКОМЕНДАЦИИ**

### **Компонент CTA Builder:**

```jsx
<CTABuilder>
  <Checkbox label="Telegram подписка">
    <Input placeholder="Текст призыва" />
    <Input placeholder="https://t.me/..." />
  </Checkbox>
  
  <Checkbox label="Сайт">
    <Input placeholder="Текст призыва" />
    <Input placeholder="https://..." />
  </Checkbox>
  
  <Checkbox label="Скидка/Акция">
    <Input placeholder="Текст акции" />
  </Checkbox>
</CTABuilder>
```

### **Превью:**

Показывай пользователю как будет выглядеть CTA:

```jsx
<Preview platform="telegram">
  {ctaItems.map((item, i) => (
    <div key={i}>{item}</div>
  ))}
</Preview>
```

---

## ✅ **ГОТОВО!**

Теперь у тебя есть гибкая система CTA:
- ✅ Каждый чекбокс = элемент массива
- ✅ AI адаптирует под платформы
- ✅ Легко добавлять/удалять элементы
- ✅ Поддержка ссылок с UTM метками

**Вопросы?** Смотри `FRONTEND_API_GUIDE.md` или тестируй в Swagger UI `/docs`

