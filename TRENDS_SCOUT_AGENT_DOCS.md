# Trends Scout Agent - Документация

## 📋 Обзор

**TrendsScoutAgent** - это специализированный AI агент для анализа трендов в реальном времени. Агент предназначен для мониторинга трендов в социальных сетях, новостях и поисковых запросах, а также для генерации контент-рекомендаций на основе выявленных трендов.

## 🎯 Функциональные возможности

### Основные функции:
- **Мониторинг трендов** в социальных сетях и новостях
- **Анализ вирусного контента** в реальном времени
- **Генерация контент-идей** на основе трендов
- **Оценка потенциала** трендов для целевой аудитории
- **Интеграция с трендовыми API** (News, Twitter, Google Trends)

### Технические характеристики:
- **Тип задач**: `TaskType.REAL_TIME` (SLA: 15 минут)
- **Специализации**: `["trend_analysis", "viral_content", "social_monitoring"]`
- **Performance score**: 1.2 (высокая скорость для real-time)
- **Максимум параллельных задач**: 3

## 🏗️ Архитектура

### Основные компоненты:

1. **TrendsScoutAgent** - основной агент
2. **TrendAnalyzer** - система анализа и скоринга трендов
3. **MCP интеграции**:
   - NewsMCP - новостные тренды
   - TwitterMCP - социальные тренды
   - GoogleTrendsMCP - поисковые тренды

### Структура данных:

```python
@dataclass
class TrendData:
    trend_id: str
    title: str
    description: str
    trend_type: TrendType  # VIRAL, NEWS, SOCIAL, SEARCH, HASHTAG
    status: TrendStatus    # RISING, PEAK, DECLINING, STABLE
    popularity_score: float      # 0-100
    engagement_rate: float       # 0-100
    growth_rate: float          # % изменения за период
    source: str
    keywords: List[str]
    hashtags: List[str]
    target_audience: List[str]
    content_ideas: List[str]
```

## 🚀 Использование

### 1. Инициализация агента

```python
from app.agents.trends_scout_agent import TrendsScoutAgent

# Создание агента
trends_agent = TrendsScoutAgent("trends_scout_001")

# Регистрация в оркестраторе
orchestrator.register_agent(trends_agent)
```

### 2. Выполнение задач

```python
from app.orchestrator.workflow_engine import Task, TaskType, TaskPriority

# Создание задачи
task = Task(
    task_id="trends_analysis_001",
    task_type=TaskType.REAL_TIME,
    priority=TaskPriority.HIGH,
    parameters={
        'analysis_type': 'general',  # general, viral_content, news_trends, social_trends
        'time_period': '1h',         # 1h, 6h, 24h, 7d
        'target_audience': 'tech_audience'  # tech_audience, business_audience, general_audience
    }
)

# Выполнение задачи
result = await trends_agent.execute_task(task)
```

### 3. API Endpoints

#### Анализ трендов
```http
POST /api/trends/analyze
Content-Type: application/json

{
    "analysis_type": "general",
    "time_period": "1h",
    "target_audience": "tech_audience"
}
```

#### Получение вирусных трендов
```http
GET /api/trends/viral
```

## 📊 Система анализа трендов

### TrendAnalyzer

Система анализа использует алгоритмы для оценки:

1. **Популярность** (0-100) - общая популярность тренда
2. **Вовлеченность** (0-100) - уровень взаимодействия аудитории
3. **Скорость роста** (%) - динамика изменения популярности
4. **Потенциал вирусности** (0-100) - вероятность стать вирусным
5. **Релевантность аудитории** (0-100) - соответствие целевой аудитории
6. **Потенциал контента** (0-100) - пригодность для создания контента

### Уровни трендов:

- **VIRAL** (81-100) - Вирусный тренд
- **HIGH** (61-80) - Высокий приоритет
- **MEDIUM** (31-60) - Средний приоритет
- **LOW** (0-30) - Низкий приоритет

### Веса для расчета общего балла:

```python
score_weights = {
    'popularity': 0.25,
    'engagement': 0.20,
    'growth_rate': 0.20,
    'virality_potential': 0.15,
    'audience_relevance': 0.10,
    'content_potential': 0.10
}
```

## 🔌 MCP интеграции

### News MCP
- **Источник**: News API
- **Функции**: Получение новостных трендов
- **Конфигурация**: `NEWS_API_KEY`

### Twitter MCP
- **Источник**: Twitter API v2
- **Функции**: Трендовые темы, вирусные твиты, анализ хештегов
- **Конфигурация**: `TWITTER_API_KEY`, `TWITTER_API_SECRET`

### Google Trends MCP
- **Источник**: Google Trends API
- **Функции**: Поисковые тренды, растущие запросы
- **Конфигурация**: Бесплатный API

## 📈 Примеры использования

### 1. Анализ общих трендов

```python
# Создание задачи для общего анализа
task = Task(
    task_id="general_trends",
    task_type=TaskType.REAL_TIME,
    parameters={
        'analysis_type': 'general',
        'time_period': '6h',
        'target_audience': 'business_audience'
    }
)

result = await trends_agent.execute_task(task)
```

### 2. Поиск вирусного контента

```python
# Создание задачи для поиска вирусного контента
task = Task(
    task_id="viral_search",
    task_type=TaskType.REAL_TIME,
    parameters={
        'analysis_type': 'viral_content',
        'time_period': '1h',
        'target_audience': 'general_audience'
    }
)

result = await trends_agent.execute_task(task)
```

### 3. Анализ новостных трендов

```python
# Создание задачи для анализа новостей
task = Task(
    task_id="news_trends",
    task_type=TaskType.REAL_TIME,
    parameters={
        'analysis_type': 'news_trends',
        'time_period': '24h',
        'target_audience': 'tech_audience'
    }
)

result = await trends_agent.execute_task(task)
```

## 🎯 Целевые аудитории

### tech_audience
- **Интересы**: технологии, IT, программирование, стартапы
- **Платформы**: telegram, youtube, habr
- **Возраст**: 18-35

### business_audience
- **Интересы**: бизнес, маркетинг, финансы, предпринимательство
- **Платформы**: linkedin, telegram, youtube
- **Возраст**: 25-45

### general_audience
- **Интересы**: развлечения, новости, образование
- **Платформы**: instagram, tiktok, youtube, telegram
- **Возраст**: 16-50

## 📋 Формат ответа

### TrendAnalysisReport

```python
@dataclass
class TrendAnalysisReport:
    report_id: str
    analysis_period: str
    total_trends: int
    trending_topics: List[TrendData]
    viral_content: List[TrendData]
    content_recommendations: List[str]
    audience_insights: Dict[str, Any]
    generated_at: datetime
```

### Пример ответа:

```json
{
    "status": "success",
    "task_id": "trends_analysis_20241207_143022",
    "agent_id": "trends_scout_001",
    "result": {
        "report_id": "trends_report_20241207_143022",
        "analysis_period": "1h",
        "total_trends": 15,
        "trending_topics": [...],
        "viral_content": [...],
        "content_recommendations": [
            "Создать контент на тему 'ИИ в образовании'",
            "Проанализировать вирусный тренд 'ChatGPT'"
        ],
        "audience_insights": {
            "total_trends": 15,
            "viral_trends_count": 3,
            "average_engagement": 72.5,
            "top_keywords": ["ИИ", "технологии", "образование"]
        }
    }
}
```

## 🧪 Тестирование

### Запуск тестов

```bash
python test_trends_scout_agent.py
```

### Тестовые сценарии:

1. **Инициализация агентов** - проверка создания и настройки
2. **Возможности агента** - проверка конфигурации и MCP интеграций
3. **Анализ трендов** - тестирование TrendAnalyzer
4. **Выполнение задач** - проверка обработки задач
5. **Анализ вирусного контента** - специализированный анализ

## ⚙️ Конфигурация

### Переменные окружения:

```bash
# News API
NEWS_API_KEY=your_news_api_key

# Twitter API
TWITTER_API_KEY=your_twitter_api_key
TWITTER_API_SECRET=your_twitter_api_secret

# Режим тестирования
TEST_MODE=True
```

### Настройки агента:

```python
# Пороги для классификации трендов
trend_thresholds = {
    'viral_min_score': 70.0,
    'trending_min_score': 50.0,
    'growth_min_rate': 20.0
}

# Время жизни кэша
cache_ttl = timedelta(minutes=15)
```

## 🚨 Обработка ошибок

### Типичные ошибки:

1. **MCP недоступен** - агент работает в режиме заглушек
2. **API лимиты** - автоматическое переключение на fallback
3. **Недостаточно данных** - снижение уровня уверенности
4. **Ошибки сети** - повторные попытки с задержкой

### Логирование:

```python
logger.info(f"TrendsScoutAgent выполняет задачу: {task.task_id}")
logger.warning("News MCP отключен - работа в режиме заглушек")
logger.error(f"Ошибка получения трендов Twitter: {e}")
```

## 🔄 Интеграция с другими агентами

### ChiefContentAgent
- Получает рекомендации по трендам для планирования контента
- Использует инсайты аудитории для стратегии

### DraftingAgent
- Создает контент на основе трендовых тем
- Использует контент-идеи от TrendsScoutAgent

### PublisherAgent
- Публикует контент в оптимальное время
- Использует данные о времени жизни трендов

## 📚 Дополнительные ресурсы

- [API Documentation](api_docs.md)
- [MCP Integration Guide](mcp_integration.md)
- [Trend Analysis Algorithms](trend_algorithms.md)
- [Performance Optimization](performance_guide.md)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи агента
2. Убедитесь в корректности API ключей
3. Проверьте доступность MCP сервисов
4. Запустите тесты для диагностики

---

**TrendsScoutAgent** - ваш надежный помощник в мире трендов! 🚀
