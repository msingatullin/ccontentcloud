# 🔐 Как тестировать Social Media API в Swagger UI

## 📍 URL Swagger UI
```
https://your-service.run.app/api/docs/
```

## 🚀 Пошаговая инструкция

### Шаг 1: Авторизация

1. Откройте Swagger UI: `/api/docs/`
2. Найдите раздел **auth** → **POST /api/v1/auth/login**
3. Нажмите **"Try it out"**
4. Введите данные для входа:
```json
{
  "email": "your@email.com",
  "password": "your_password"
}
```
5. Нажмите **"Execute"**
6. **Скопируйте** значение `access_token` из ответа (без кавычек!)

### Шаг 2: Добавьте токен в Swagger

1. **Нажмите кнопку 🔒 "Authorize"** в правом верхнем углу Swagger UI
2. В появившемся окне введите **ТОЛЬКО токен БЕЗ слова Bearer**:
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```
**ВАЖНО:** 
- ❌ НЕ добавляйте слово `Bearer` 
- ❌ НЕ добавляйте кавычки
- ✅ Вставьте только сам токен - Swagger автоматически добавит `Bearer` префикс

3. Нажмите **"Authorize"**
4. Нажмите **"Close"**

### Шаг 3: Тестируйте Social Media endpoints

Теперь можно тестировать:

#### GET /api/v1/social-media/accounts
Получить все социальные сети пользователя

1. Найдите **social-media** → **GET /api/v1/social-media/accounts**
2. Нажмите **"Try it out"**
3. Нажмите **"Execute"**
4. Должен вернуться ответ 200 OK с данными:
```json
{
  "success": true,
  "data": [
    {
      "name": "Telegram",
      "isActive": true,
      "metadata": {
        "channelLink": "https://t.me/channel",
        "accountId": 1,
        "isDefault": true
      }
    }
  ]
}
```

#### PUT /api/v1/social-media/accounts
Обновить настройки социальной сети

1. Найдите **social-media** → **PUT /api/v1/social-media/accounts**
2. Нажмите **"Try it out"**
3. Введите тело запроса:
```json
{
  "name": "Telegram",
  "isActive": true,
  "metadata": {
    "accountId": 1,
    "isDefault": true
  }
}
```
4. Нажмите **"Execute"**
5. Должен вернуться ответ 200 OK

## ❌ Типичные ошибки

### 500 Internal Server Error (до деплоя исправлений)
Если вы видите 500 ошибку - значит еще не задеплоена последняя версия с обработчиками JWT ошибок. Дождитесь автодеплоя.

### 401 Unauthorized
```json
{
  "error": "Unauthorized",
  "message": "Требуется авторизация. Нажмите кнопку 'Authorize' в Swagger UI и введите токен",
  "details": "Missing 'Bearer' type in 'Authorization' header..."
}
```
**Решение:** 
1. Нажмите кнопку **🔒 "Authorize"** вверху справа в Swagger UI
2. Введите токен в формате: `Bearer <ваш_токен>`
3. Нажмите "Authorize" и "Close"

**Проверьте:**
- Вы сделали login и получили `access_token`?
- Скопировали токен БЕЗ кавычек?
- ❌ НЕ добавляли слово `Bearer` вручную (Swagger добавит автоматически)
- ✅ Ввели только сам токен в поле авторизации

### 401 Invalid token
```json
{
  "error": "Invalid token",
  "message": "Недействительный токен авторизации"
}
```
**Решение:** 
- Проверьте что скопировали токен правильно (без кавычек, без лишних пробелов)
- ❌ НЕ добавляйте `Bearer` вручную (Swagger сам добавит)
- ✅ Вставьте только токен в поле авторизации
- Токен не истек (они живут 30 минут)

### 401 Token expired
```json
{
  "error": "Token expired",
  "message": "Токен авторизации истек. Пожалуйста, войдите снова"
}
```
**Решение:** Сделайте новый login и получите новый токен

## 📝 Примечания

- Токены живут **30 минут**
- После этого нужно делать новый `/auth/login`
- Или использовать `/auth/refresh` с refresh_token
- `socialMedia` всегда возвращается при login, даже если социальные сети не подключены

## 🎯 Готово!

Теперь вы можете тестировать все Social Media endpoints в Swagger UI с правильной авторизацией.

