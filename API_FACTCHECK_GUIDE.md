# API Guide: Создание контента с фактчекингом

## 📋 Обзор

Данное руководство описывает правильную структуру API запросов для создания контента с автоматической проверкой фактов через ResearchFactCheckAgent.

## 🔧 API Endpoints

### Базовый URL
```
http://localhost:5000/api
```

### Основные endpoints:
- `POST /content/create` - Создание контента с фактчекингом
- `GET /workflow/{id}/status` - Статус выполнения workflow
- `GET /agents/status` - Статус всех агентов
- `GET /health` - Проверка здоровья API

## 📝 Структура запроса ContentCreateRequest

### Обязательные поля:

```json
{
  "title": "string",                    // Заголовок контента (1-200 символов)
  "description": "string",              // Описание контента (10-1000 символов)
  "target_audience": "string",          // Целевая аудитория (1-200 символов)
  "business_goals": ["string"],         // Бизнес-цели (1-10 элементов)
  "call_to_action": "string",           // Призыв к действию (1-200 символов)
  "platforms": ["string"]               // Платформы (1-5 элементов)
}
```

### Опциональные поля:

```json
{
  "tone": "professional",               // Тон контента (professional/casual/friendly/authoritative)
  "keywords": ["string"],               // Ключевые слова (до 20 элементов)
  "content_types": ["post"],            // Типы контента (post/story/reel/thread/longread/carousel/video/image/poll/quiz)
  "constraints": {},                    // Дополнительные ограничения
  "test_mode": true                     // Тестовый режим (без реальной публикации)
}
```

### Поддерживаемые платформы:
- `telegram`, `vk`, `instagram`, `twitter`, `youtube`, `tiktok`, `linkedin`, `facebook`, `blog`

### Поддерживаемые типы контента:
- `post`, `story`, `reel`, `thread`, `longread`, `carousel`, `video`, `image`, `poll`, `quiz`

## 🔍 Активация фактчекинга

Для включения автоматической проверки фактов добавьте в `constraints`:

```json
{
  "constraints": {
    "fact_checking": true,              // Включить фактчекинг
    "max_length": 1000,                 // Максимальная длина текста
    "include_statistics": true,         // Включить статистические данные
    "verify_quotes": true,              // Проверять цитаты
    "check_dates": true                 // Проверять даты
  }
}
```

## 📊 Пример корректного запроса

### Полный пример с фактчекингом:

```json
{
  "title": "Статистика использования AI в бизнесе: 90% компаний уже внедряют технологии",
  "description": "Анализ влияния искусственного интеллекта на современный бизнес. В 2023 году произошла революция в области AI. Исследования показывают, что 90% компаний уже используют AI технологии. Ученые подтвердили, что AI увеличивает производительность на 40%.",
  "target_audience": "IT-специалисты и бизнес-лидеры",
  "business_goals": [
    "привлечение внимания к инновациям в AI",
    "образование аудитории о статистике внедрения AI",
    "установление экспертного авторитета в области технологий"
  ],
  "call_to_action": "Подписывайтесь на наш канал для получения актуальной статистики по AI",
  "tone": "professional",
  "keywords": ["AI", "искусственный интеллект", "статистика", "бизнес", "технологии", "2023"],
  "platforms": ["telegram", "vk"],
  "content_types": ["post"],
  "constraints": {
    "fact_checking": true,
    "max_length": 1000,
    "include_statistics": true,
    "verify_quotes": true,
    "check_dates": true
  },
  "test_mode": true
}
```

## 🚀 Curl команды для тестирования

### 1. Быстрый тест:

```bash
curl -X POST http://localhost:5000/api/content/create \
  -H "Content-Type: application/json" \
  -d '{
    "title": "AI статистика: 90% компаний используют технологии",
    "description": "В 2023 году произошла революция в AI. Исследования показывают, что 90% компаний уже используют AI технологии.",
    "target_audience": "IT-специалисты",
    "business_goals": ["привлечение внимания к AI"],
    "call_to_action": "Подписывайтесь для получения статистики",
    "tone": "professional",
    "keywords": ["AI", "статистика", "2023"],
    "platforms": ["telegram"],
    "content_types": ["post"],
    "constraints": {"fact_checking": true},
    "test_mode": true
  }'
```

### 2. Проверка статуса агентов:

```bash
curl http://localhost:5000/api/agents/status
```

### 3. Проверка здоровья API:

```bash
curl http://localhost:5000/api/health
```

### 4. Отслеживание workflow:

```bash
curl http://localhost:5000/api/workflow/{workflow_id}/status
```

## 📈 Структура ответа

### Успешный ответ:

```json
{
  "success": true,
  "workflow_id": "47fa6a19-6050-4707-a716-0f2260700b4e",
  "brief_id": "8aa7317b-548a-45dd-871a-f0c130fbc7e3",
  "result": {
    "workflow_id": "47fa6a19-6050-4707-a716-0f2260700b4e",
    "status": "completed",
    "completed_tasks": 6,
    "failed_tasks": 0,
    "total_tasks": 6,
    "results": {
      "task_factcheck": {
        "task_id": "task_factcheck",
        "agent_id": "research_factcheck_agent",
        "status": "completed",
        "timestamp": "2025-09-07T17:30:00Z",
        "fact_check_report": {
          "content_id": "content_123",
          "total_claims": 3,
          "verified_claims": 1,
          "disputed_claims": 0,
          "false_claims": 0,
          "overall_confidence": 0.65,
          "recommendations": [
            "Статистическое утверждение частично подтверждено",
            "Рекомендуется добавить дополнительные источники"
          ]
        },
        "detailed_results": [
          {
            "claim": "90% компаний уже используют AI технологии",
            "type": "statistical",
            "status": "partially_verified",
            "confidence": 0.60,
            "sources": ["wikipedia:test_article_1"],
            "evidence": ["Wikipedia: Тестовая статья 1 - информация о статистике"],
            "recommendations": ["Статистическое утверждение частично подтверждено"]
          }
        ]
      }
    }
  },
  "timestamp": "2025-09-07T17:30:00Z"
}
```

### Ответ с ошибкой:

```json
{
  "error": "Validation Error",
  "message": "Некорректные данные запроса",
  "status_code": 400,
  "timestamp": "2025-09-07T17:30:00Z",
  "details": [
    {
      "field": "title",
      "message": "Поле обязательно для заполнения",
      "type": "missing"
    }
  ]
}
```

## 🔍 Результаты фактчекинга

### Структура fact_check_report:

```json
{
  "fact_check_report": {
    "content_id": "string",             // ID контента
    "total_claims": 0,                  // Всего утверждений
    "verified_claims": 0,               // Проверенных утверждений
    "disputed_claims": 0,               // Спорных утверждений
    "false_claims": 0,                  // Ложных утверждений
    "overall_confidence": 0.0,          // Общая уверенность (0.0-1.0)
    "recommendations": ["string"]       // Рекомендации
  }
}
```

### Структура detailed_results:

```json
{
  "detailed_results": [
    {
      "claim": "string",                // Текст утверждения
      "type": "statistical",            // Тип утверждения
      "status": "verified",             // Статус проверки
      "confidence": 0.0,                // Уверенность (0.0-1.0)
      "sources": ["string"],            // Источники верификации
      "evidence": ["string"],           // Доказательства
      "recommendations": ["string"]     // Рекомендации
    }
  ]
}
```

### Типы утверждений:
- `statistical` - Статистические данные
- `temporal` - Временные утверждения (даты)
- `quote` - Цитаты
- `scientific` - Научные утверждения
- `historical` - Исторические факты
- `general` - Общие утверждения

### Статусы проверки:
- `verified` - Подтверждено
- `partially_verified` - Частично подтверждено
- `unverified` - Не проверено
- `disputed` - Спорно
- `false` - Ложно

## 🧪 Тестирование

### Запуск тестов:

```bash
# Python тест
python test_api_factcheck.py

# Полный curl тест
./test_factcheck_curl.sh

# Быстрый curl тест
./quick_test_curl.sh
```

### Проверка интеграции:

1. **Проверьте статус агентов** - убедитесь что ResearchFactCheckAgent зарегистрирован
2. **Создайте контент с фактчекингом** - используйте `"fact_checking": true` в constraints
3. **Отследите выполнение** - проверьте статус workflow
4. **Проанализируйте результаты** - изучите fact_check_report и detailed_results

## ⚠️ Важные замечания

1. **Обязательные поля** - title, description, target_audience, business_goals, call_to_action, platforms
2. **Валидация** - все поля проходят строгую валидацию
3. **Фактчекинг** - активируется только при `"fact_checking": true` в constraints
4. **Тестовый режим** - используйте `"test_mode": true` для безопасного тестирования
5. **Таймауты** - API имеет таймауты, длительные операции выполняются асинхронно

## 🔧 Troubleshooting

### Частые ошибки:

1. **400 Bad Request** - проверьте обязательные поля и валидацию
2. **500 Internal Server Error** - проверьте логи сервера
3. **Агент не найден** - убедитесь что ResearchFactCheckAgent зарегистрирован
4. **Фактчекинг не работает** - проверьте `"fact_checking": true` в constraints

### Логи для диагностики:

```bash
# Проверка логов агента
tail -f logs/research_factcheck_agent.log

# Проверка логов API
tail -f logs/api.log

# Проверка логов оркестратора
tail -f logs/orchestrator.log
```

---

**Статус:** ✅ API готов к использованию  
**Версия:** 1.0.0  
**Дата:** 2025-09-07
