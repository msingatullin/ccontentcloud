# AI Post Creator - Генератор постов для социальных сетей

Сервис для создания постов для социальных сетей с использованием искусственного интеллекта. Превращает любой контент в идеальные посты за секунды.

## 🚀 Основные возможности

### 1. **Создание постов из URL** 
- Веб-скрапинг любых веб-страниц
- Извлечение основного контента, заголовков, изображений
- Автоматическое определение автора и даты публикации

### 2. **Создание из текста**
- Вставка любого текста для генерации поста
- Поддержка длинных текстов с автоматическим сокращением
- Сохранение ключевых моментов

### 3. **Извлечение аудио из видео**
- Поддержка форматов: MP4, AVI, MOV, WebM (до 500MB)
- Автоматическое извлечение аудиодорожки
- Транскрибация в текст

### 4. **Транскрибация аудио**
- Поддержка форматов: MP3, WAV, M4A, OGG (до 100MB)
- Высокоточное распознавание речи
- Преобразование в структурированный текст

### 5. **Предпросмотр для разных платформ**
- Instagram
- Twitter/X
- LinkedIn
- VKontakte
- Facebook
- TikTok

## 🛠 Технологии

- **Frontend**: React 18, TypeScript, Vite
- **UI**: Tailwind CSS, shadcn/ui компоненты
- **Routing**: React Router v6
- **State**: React hooks
- **API**: REST API с поддержкой офлайн режима (mock данные) и интеграцией с OpenAI API

## 📦 Установка и запуск

### Быстрый старт:
```bash
# Установка всех зависимостей
npm install && npm run setup:server

# Создание файла окружения
cp .env.example .env.local

# Настройте свои API ключи в файле .env.local
# Для работы транскрибации видео необходимо указать VITE_OPENAI_API_KEY

# Запуск всего проекта (frontend + backend)
npm run dev:all
```

### Раздельный запуск:
```bash
# Frontend только
npm run dev

# Backend только  
npm run dev:server

# Сборка для продакшена
npm run build

# Предпросмотр production сборки
npm run preview
```

### Порты:
- Frontend: http://localhost:8080
- API сервер: http://localhost:3001

## 🏗 Структура проекта

```
├── server/           # Backend для веб-скрапинга
│   ├── scraper.js    # Express сервер с Cheerio
│   └── package.json  # Зависимости сервера
├── src/              # Frontend приложение
│   ├── components/   # React компоненты
│   │   ├── ui/       # UI компоненты (shadcn)
│   │   ├── Header.tsx    # Шапка сайта
│   │   ├── HeroSection.tsx # Главная секция
│   │   ├── PostPreview.tsx # Превью постов
│   │   ├── UrlScraper.tsx  # Компонент веб-скрапинга
│   │   └── MediaUploader.tsx # Загрузка медиафайлов
│   ├── pages/        # Страницы приложения
│   │   ├── Index.tsx     # Главная страница
│   │   └── Generator.tsx # Страница генератора
│   ├── hooks/        # Custom React hooks
│   │   └── usePostGenerator.ts # Hook для работы с API
│   ├── lib/          # Утилиты и API
│   │   ├── utils.ts      # Общие утилиты
│   │   └── api.ts        # API сервис
│   └── types/        # TypeScript типы
│       └── post-generator.ts # Типы для генератора
└── vite.config.ts    # Конфигурация с прокси для API
```

## 💰 Настройка API ключей

Для полноценной работы приложения, особенно функций транскрибации видео и аудио, необходимо настроить следующие ключи в файле `.env.local`:

```
# Обязательный ключ для транскрибации
VITE_OPENAI_API_KEY=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

# Настройки сервера API (опционально)
VITE_API_BASE_URL=http://localhost:3001/api
VITE_API_KEY=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
VITE_API_ENABLED=true
```

Если у вас нет ключа OpenAI API, приложение все равно будет работать, но вместо реальной транскрибации будут использоваться демонстрационные данные.

## 🔧 API Endpoints

### Реализованные:
- `POST /api/scrape` - Извлечение контента из URL ✅
- `GET /api/health` - Проверка состояния сервера ✅

### Планируемые:
- `POST /api/generate` - Генерация поста с помощью AI
- `POST /api/transcribe` - Транскрибация медиафайлов
- `POST /api/optimize` - Оптимизация поста под платформу

## 🚀 Автоматический деплой на GitHub Pages

Проект настроен на автоматический деплой через GitHub Actions. При каждом push в ветку `main` происходит сборка и публикация на GitHub Pages.

### Настройка:

1. **Включите GitHub Pages** в настройках репозитория:
   - Перейдите в `Settings > Pages`
   - В разделе "Source" выберите `Deploy from a branch`
   - В разделе "Branch" выберите `gh-pages` и сохраните настройки

2. **Добавьте базовый путь** в vite.config.ts, если ваш репозиторий не размещается в корне домена:
   ```js
   base: '/имя-репозитория/', // Например: '/dev-buddy-clone/'
   ```

3. **Обновите маршруты** в React Router, если вы используете базовый путь:
   ```jsx
   <BrowserRouter basename="/имя-репозитория/">
   ```

4. **Секреты для деплоя** (настраиваются в GitHub):
   - Перейдите в `Settings > Secrets and variables > Actions`
   - Добавьте необходимые секреты:
     - `GITHUB_TOKEN` (генерируется автоматически)
     - `VITE_OPENAI_API_KEY` (если требуется для сборки)

5. **Тестирование деплоя**:
   - Отправьте изменения в репозиторий: `git push origin main`
   - Проверьте вкладку "Actions" для отслеживания процесса сборки
   - После успешного деплоя сайт будет доступен по адресу: `https://username.github.io/имя-репозитория/`

## 🎯 Использование

1. **Быстрый старт с URL**: Вставьте ссылку на главной странице
2. **Расширенный режим**: Перейдите в генератор для доступа ко всем функциям
3. **Выберите источник**: URL, текст, видео или аудио
4. **Настройте параметры**: Добавьте контекст при необходимости
5. **Генерируйте**: Нажмите "Создать пост"
6. **Редактируйте**: Отредактируйте результат при необходимости
7. **Предпросмотр**: Посмотрите как пост будет выглядеть в разных соцсетях

## 🚀 Статус разработки

### ✅ Реализовано:
- [x] Базовый веб-скрапинг через Express + Cheerio
- [x] Извлечение контента из URL с метаданными
- [x] Превью постов для разных соцсетей
- [x] Загрузка медиафайлов (UI)
- [x] Структура для AI интеграции
- [x] Автоматический деплой через GitHub Actions

### 🔄 В разработке:
- [x] Полная интеграция с OpenAI API
- [x] Транскрибация через Whisper API
- [ ] Обработка динамических сайтов (Puppeteer)

### 📋 Планируется:
- [ ] Автопостинг в социальные сети
- [ ] Сохранение истории постов
- [ ] Шаблоны постов
- [ ] Массовая генерация
- [ ] Аналитика эффективности
- [ ] Планировщик публикаций
- [ ] A/B тестирование постов

## 📝 Лицензия

MIT