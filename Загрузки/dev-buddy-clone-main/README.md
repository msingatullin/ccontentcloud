# Dev Buddy

Интеллектуальный помощник для разработчиков контента с интеграцией AI.

## 🚀 Статус разработки

### ✅ Реализовано:
- [x] Базовый веб-скрапинг через Express + Cheerio
- [x] Извлечение контента из URL с метаданными
- [x] Превью постов для разных соцсетей
- [x] Загрузка медиафайлов (UI)
- [x] Структура для AI интеграции
- [x] Автоматический деплой через GitHub Actions

### 🔄 В разработке:
- [x] Полная интеграция с OpenAI API
- [x] Транскрибация через Whisper API
- [ ] Обработка динамических сайтов (Puppeteer)

## 🔧 API Endpoints

### Реализованные:
- `POST /api/scrape` - Извлечение контента из URL ✅
- `GET /api/health` - Проверка состояния сервера ✅

### Планируемые:
- `POST /api/generate` - Генерация поста с помощью AI
- `POST /api/transcribe` - Транскрибация медиафайлов
- `POST /api/optimize` - Оптимизация поста под платформу

## 🚀 Автоматический деплой на GitHub Pages

Проект настроен на автоматический деплой через GitHub Actions. При каждом push в ветку `main` происходит сборка и публикация на GitHub Pages.

### Настройка:

1. **Включите GitHub Pages** в настройках репозитория:
   - Перейдите в раздел Settings > Pages
   - В разделе "Source" выберите "Deploy from a branch"
   - Выберите ветку "gh-pages" и папку "/ (root)"
   - Нажмите Save

2. **Проверьте доступ Actions** к GitHub Pages:
   - Перейдите в раздел Settings > Actions > General
   - Убедитесь, что "Read and write permissions" включены в разделе "Workflow permissions"
   - Нажмите Save

3. **Важные секреты репозитория**:
   - При необходимости добавьте секреты в Settings > Secrets and variables > Actions
   - Например, для API ключей (API_KEY) или других конфиденциальных данных

### Использование:

После настройки каждый push в ветку `main` автоматически запускает процесс деплоя:
1. Собирает приложение с помощью Vite
2. Публикует результаты сборки на GitHub Pages
3. Приложение становится доступно по адресу https://username.github.io/repo-name/

### Локальная разработка:

```bash
# Установка зависимостей
npm install

# Запуск для разработки
npm run dev

# Сборка для продакшн
npm run build

# Предпросмотр сборки
npm run preview
```

## 📝 Лицензия

MIT