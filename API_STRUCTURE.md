# 📋 Структура API Content Curator Cloud

**Базовый URL:** `https://content-curator-1046574462613.us-central1.run.app`  
**Версия API:** `v1`  
**Префикс:** `/api/v1`

---

## 🔐 Аутентификация

Все защищенные endpoints требуют JWT токен в заголовке:
```http
Authorization: Bearer <JWT_TOKEN>
```
или просто:
```http
Authorization: <JWT_TOKEN>
```

Токен получается через `/api/v1/auth/login` или `/api/v1/auth/register`.

---

## 📚 Категории API

### 1. 🔑 Аутентификация (`/api/v1/auth`)

#### Регистрация и авторизация
- `POST /api/v1/auth/register` - Регистрация нового пользователя
- `POST /api/v1/auth/login` - Авторизация пользователя
- `POST /api/v1/auth/logout` - Выход из системы
- `POST /api/v1/auth/logout-all` - Выход из всех устройств

#### Email верификация
- `POST /api/v1/auth/verify-email` - Подтверждение email
- `POST /api/v1/auth/resend-verification` - Повторная отправка письма верификации

#### Сброс пароля
- `POST /api/v1/auth/forgot-password` - Запрос на сброс пароля
- `POST /api/v1/auth/reset-password` - Сброс пароля по токену

#### Управление профилем
- `GET /api/v1/auth/me` - Получить информацию о текущем пользователе (включая socialMedia)
- `PUT /api/v1/auth/profile` - Обновить профиль
- `POST /api/v1/auth/change-password` - Изменить пароль

#### Управление сессиями
- `GET /api/v1/auth/sessions` - Список активных сессий
- `DELETE /api/v1/auth/sessions/<session_id>` - Завершить сессию

#### Обновление токенов
- `POST /api/v1/auth/refresh` - Обновить access token

---

### 2. 📝 Контент (`/api/v1/content`)

#### Создание контента
- `POST /api/v1/content/create` - Создать контент через AI агентов
  - **Параметры:**
    - `title` (string, required) - Заголовок контента (1-200 символов)
    - `description` (string, required) - Описание/промпт (10-1000 символов)
    - `target_audience` (string, required) - Целевая аудитория (1-200 символов)
    - `business_goals` (array, required) - Бизнес-цели (1-10 элементов)
    - `platforms` (array, optional) - Платформы: ["telegram", "vk", "instagram", "twitter"]
    - `content_types` (array, optional) - Типы: ["post", "story", "article", "video", "image", "reel"]
    - `tone` (string, optional) - Тон: "professional", "casual", "friendly", "authoritative"
    - `keywords` (array, optional) - Ключевые слова (до 20 элементов)
    - `call_to_action` (array, optional) - Призывы к действию
    - `test_mode` (boolean, optional) - Тестовый режим (по умолчанию false)
    - `channel_id` (integer, optional) - ID конкретного канала/аккаунта
    - `uploaded_files` (array, optional) - IDs загруженных файлов
    - `reference_urls` (array, optional) - Референсные URL
  - **Ответ:** `workflow_id`, `brief_id`, `result` (статус выполнения)

- `GET /api/v1/content/example` - Пример запроса на создание контента

#### История контента
- `GET /api/v1/content/history` - Получить историю созданного контента
  - **Параметры:**
    - `page` (int, optional) - Номер страницы (по умолчанию 1)
    - `per_page` (int, optional) - Элементов на странице (по умолчанию 10)
    - `platform` (string, optional) - Фильтр по платформе
    - `date_from` (string, optional) - Дата начала (ISO format)
    - `date_to` (string, optional) - Дата окончания (ISO format)

- `GET /api/v1/content/<content_id>` - Получить детальную информацию о контенте
  - **Ответ включает:** токены, стоимость, метрики

- `DELETE /api/v1/content/<content_id>` - Удалить контент из истории

---

### 3. 📤 Загрузка файлов (`/api/v1/uploads`)

#### Загрузка
- `POST /api/v1/uploads/upload` - Загрузить один файл
  - **Content-Type:** `multipart/form-data`
  - **Параметры:**
    - `file` (file, required) - Файл для загрузки
    - `folder` (string, optional) - Папка: "images", "documents", "videos"
    - `analyze` (boolean, optional) - Анализировать через AI

- `POST /api/v1/uploads/batch` - Загрузить несколько файлов (до 20 файлов)
  - **Content-Type:** `multipart/form-data`
  - **Параметры:**
    - `files` (file[], required) - Файлы для загрузки
    - `folder` (string, optional) - Папка
    - `analyze` (boolean, optional) - Анализировать через AI

#### Управление файлами
- `GET /api/v1/uploads/list` - Список загруженных файлов
  - **Параметры:**
    - `page` (int, optional) - Номер страницы
    - `per_page` (int, optional) - Файлов на странице (по умолчанию 20)
    - `file_type` (string, optional) - Фильтр: "image", "document", "video"

- `GET /api/v1/uploads/<file_id>` - Детальная информация о файле

- `DELETE /api/v1/uploads/<file_id>` - Удалить файл

**Лимиты:**
- Макс размер файла: 100 MB
- Макс файлов в batch: 20
- Форматы: jpg, png, gif, webp, pdf, docx, xlsx, md, txt, mp4, mov, avi

---

### 4. 🔄 Workflow (`/api/v1/workflow`)

- `GET /api/v1/workflow/<workflow_id>/status` - Получить статус workflow
  - **Ответ:** статус, результаты задач, метрики

- `POST /api/v1/workflow/<workflow_id>/cancel` - Отменить выполнение workflow

---

### 5. 🤖 Агенты (`/api/v1/agents`)

- `GET /api/v1/agents/status` - Получить статус всех агентов или конкретного агента
  - **Параметры:**
    - `agent_id` (string, optional) - ID конкретного агента

- `GET /api/v1/agents/<agent_id>/tasks` - Получить список задач конкретного агента

- `POST /api/v1/agents/restart-all` - Перезапустить все агенты

**Доступные агенты:**
- `chief_content_agent` - Стратег контента
- `drafting_agent` - Генератор текста (GPT-5-mini)
- `publisher_agent` - Публикатор контента
- `repurpose_agent` - Адаптер контента
- `trends_scout_agent` - Аналитик трендов
- `research_factcheck_agent` - Фактчекер
- `multimedia_producer_agent` - Генератор изображений
- `legal_guard_agent` - Юридический проверщик
- `community_concierge_agent` - Менеджер сообщества
- `paid_creative_agent` - Рекламный креатив

---

### 6. 📊 Система (`/api/v1/system`)

- `GET /api/v1/system/status` - Получить общий статус системы
- `GET /api/v1/system/health` - Проверка здоровья системы
- `GET /api/v1/system/metrics` - Получить метрики системы

---

### 7. 💰 Биллинг (`/api/v1/billing`)

#### Тарифные планы
- `GET /api/v1/billing/plans` - Получить все доступные тарифные планы
- `GET /api/v1/billing/plans/<plan_id>` - Получить конкретный тарифный план

#### Подписки
- `GET /api/v1/billing/subscription` - Получить подписку пользователя
- `POST /api/v1/billing/subscription` - Создать подписку
- `POST /api/v1/billing/subscription/<subscription_id>/cancel` - Отменить подписку

#### Использование
- `GET /api/v1/billing/usage` - Получить статистику использования

#### Платежи
- `GET /api/v1/billing/payment-methods` - Получить доступные способы оплаты
- `GET /api/v1/billing/payment/<payment_id>` - Получить статус платежа

#### События
- `GET /api/v1/billing/events` - Получить события billing системы

---

### 8. 📊 Использование токенов (`/api/v1/billing/usage/tokens`)

#### Сводка
- `GET /api/v1/billing/usage/tokens/summary` - Сводка по токенам (сегодня, месяц, всего)

#### История
- `GET /api/v1/billing/usage/tokens/history` - История по дням (для графиков)
  - **Параметры:**
    - `days` (int, optional) - Количество дней (по умолчанию 30)
    - `agent_id` (string, optional) - Фильтр по агенту

#### Статистика по агентам
- `GET /api/v1/billing/usage/tokens/by-agent` - Расход токенов по агентам
  - **Параметры:**
    - `days` (int, optional) - Количество дней (по умолчанию 30)

#### Статистика по моделям
- `GET /api/v1/billing/usage/tokens/by-model` - Расход токенов по AI моделям
  - **Параметры:**
    - `days` (int, optional) - Количество дней (по умолчанию 30)

#### Детальная статистика
- `GET /api/v1/billing/usage/tokens/detailed` - Детальная статистика с фильтрами
  - **Параметры:**
    - `days` (int, optional) - Количество дней
    - `agent_id` (string, optional) - Фильтр по агенту
    - `ai_model` (string, optional) - Фильтр по модели (gpt-5-mini, gpt-5, dall-e-3)
    - `platform` (string, optional) - Фильтр по платформе
    - `page` (int, optional) - Номер страницы
    - `per_page` (int, optional) - Элементов на странице

---

### 9. 📱 Социальные сети (`/api/v1/social-media`)

#### Общие endpoints
- `GET /api/v1/social-media/accounts` - Получить все подключенные аккаунты
  - **Ответ:** список аккаунтов Telegram, Instagram, Twitter с метаданными
- `PUT /api/v1/social-media/accounts` - Обновить аккаунт социальной сети

#### Telegram (`/api/v1/telegram`)
- `GET /api/v1/telegram/bot-info` - Информация о боте
- `GET /api/v1/telegram/channels` - Список каналов пользователя
- `POST /api/v1/telegram/channels` - Добавить канал
  - **Параметры:**
    - `channel_username` (string, required) - @username или username канала
    - `channel_name` (string, optional) - Название канала
    - `is_default` (boolean, optional) - Установить как дефолтный
- `GET /api/v1/telegram/channels/<channel_id>` - Информация о канале
- `PUT /api/v1/telegram/channels/<channel_id>` - Обновить канал
- `PUT /api/v1/telegram/channels/<channel_id>/default` - Установить канал как дефолтный
- `PUT /api/v1/telegram/channels/<channel_id>/activate` - Активировать/деактивировать канал
- `POST /api/v1/telegram/channels/<channel_id>/verify` - Проверить канал
- `DELETE /api/v1/telegram/channels/<channel_id>` - Удалить канал

#### Instagram (`/api/v1/instagram`)
- `GET /api/v1/instagram/accounts` - Список аккаунтов Instagram
- `POST /api/v1/instagram/accounts` - Добавить аккаунт Instagram
- `GET /api/v1/instagram/accounts/<account_id>` - Информация об аккаунте
- `PUT /api/v1/instagram/accounts/<account_id>/default` - Установить как дефолтный
- `PUT /api/v1/instagram/accounts/<account_id>/activate` - Активировать/деактивировать
- `DELETE /api/v1/instagram/accounts/<account_id>` - Удалить аккаунт
- `GET /api/v1/instagram/info` - Информация об интеграции Instagram

#### Twitter (`/api/v1/twitter`)
- `GET /api/v1/twitter/oauth/url` - Получить OAuth URL для авторизации
- `GET /api/v1/twitter/oauth/callback` - OAuth callback
- `GET /api/v1/twitter/accounts` - Список аккаунтов Twitter
- `GET /api/v1/twitter/accounts/<account_id>` - Информация об аккаунте
- `PUT /api/v1/twitter/accounts/<account_id>/default` - Установить как дефолтный
- `PUT /api/v1/twitter/accounts/<account_id>/activate` - Активировать/деактивировать
- `DELETE /api/v1/twitter/accounts/<account_id>` - Удалить аккаунт
- `GET /api/v1/twitter/info` - Информация об интеграции Twitter

---

### 10. ❤️ Health Check (`/health`)

- `GET /health` - Проверка здоровья API (без аутентификации)

---

## 📋 Формат ответов

### Успешный ответ
```json
{
  "success": true,
  "data": { ... },
  "timestamp": "2025-11-04T12:00:00"
}
```

### Ошибка
```json
{
  "success": false,
  "error": "Error Type",
  "message": "Описание ошибки",
  "status_code": 400,
  "timestamp": "2025-11-04T12:00:00"
}
```

---

## 🔄 Типичный workflow создания контента

1. **Авторизация:**
   ```http
   POST /api/v1/auth/login
   ```
   Получить `access_token`

2. **Загрузка файлов (опционально):**
   ```http
   POST /api/v1/uploads/batch
   ```
   Получить `file_id` для каждого файла

3. **Создание контента:**
   ```http
   POST /api/v1/content/create
   Authorization: Bearer <token>
   ```
   Получить `workflow_id`

4. **Проверка статуса:**
   ```http
   GET /api/v1/workflow/<workflow_id>/status
   ```

5. **Получение результата:**
   ```http
   GET /api/v1/content/<content_id>
   ```

---

## 📊 Статусы кодов

- `200` - Успешный запрос
- `201` - Ресурс создан
- `400` - Ошибка валидации
- `401` - Не авторизован
- `403` - Доступ запрещен
- `404` - Ресурс не найден
- `500` - Внутренняя ошибка сервера

---

## 🔗 Swagger UI

Интерактивная документация доступна по адресу:
```
https://content-curator-1046574462613.us-central1.run.app/api/docs
```

---

## 📝 Примечания

- Все даты в формате ISO 8601: `YYYY-MM-DDTHH:MM:SS`
- Все цены в рублях (RUB) с курсом ~95₽/$ (обновляется автоматически)
- Лимиты токенов учитываются автоматически
- Тестовый режим (`test_mode=true`) не публикует реально, только валидирует

---

**Последнее обновление:** 4 ноября 2025  
**Версия:** 1.0

