# 📱 Telegram Channels - Руководство по настройке

## Архитектура: ОДИН БОТ - МНОГО КАНАЛОВ

Система использует одного бота (@content4ubot) для публикации во все каналы клиентов.

---

## 🤖 Информация о боте

- **Имя:** Content4u
- **Username:** @content4ubot
- **Ссылка:** https://t.me/content4ubot
- **Токен:** Хранится в переменной окружения `TELEGRAM_BOT_TOKEN`

---

## 🚀 Быстрый старт

### 1. Настройка токена бота

Добавьте токен в `.env` файл:

```bash
TELEGRAM_BOT_TOKEN=7579380481:AAFAiTobG-PFv7Wgr2VC-BioWcnGsaQZafc
```

⚠️ **ВАЖНО:** Не коммитьте файл `.env` с реальным токеном в Git!

### 2. Применение миграции БД

Выполните миграцию для создания таблицы `telegram_channels`:

```bash
# SQLite
sqlite3 content_curator.db < migrations/add_telegram_channels.sql

# Или через Python
python3 -c "
from app.database.connection import engine
from app.models.telegram_channels import TelegramChannel
TelegramChannel.metadata.create_all(engine)
"
```

### 3. Проверка настройки

Запустите тест:

```bash
python3 -m pytest tests/test_telegram_channels.py -v
```

---

## 📋 API Endpoints

### Получить информацию о боте
```
GET /api/telegram/bot-info
Authorization: Bearer {jwt_token}
```

### Получить список каналов пользователя
```
GET /api/telegram/channels
Authorization: Bearer {jwt_token}
```

### Добавить новый канал
```
POST /api/telegram/channels
Authorization: Bearer {jwt_token}
Content-Type: application/json

{
  "channel_link": "https://t.me/mychannel",
  "channel_name": "Мой финансовый канал"
}
```

### Установить канал по умолчанию
```
PUT /api/telegram/channels/{channel_id}/default
Authorization: Bearer {jwt_token}
```

### Удалить канал
```
DELETE /api/telegram/channels/{channel_id}
Authorization: Bearer {jwt_token}
```

### Проверить статус канала
```
POST /api/telegram/channels/{channel_id}/verify
Authorization: Bearer {jwt_token}
```

---

## 👤 Инструкция для клиента

### Как подключить свой канал:

1. **Откройте бота в Telegram**
   - Перейдите по ссылке: https://t.me/content4ubot
   - Или найдите: @content4ubot

2. **Добавьте бота в свой канал**
   - Откройте настройки вашего канала
   - Перейдите в "Администраторы"
   - Нажмите "Добавить администратора"
   - Найдите @content4ubot и добавьте его

3. **Выдайте права боту**
   - ✅ Публикация сообщений
   - ✅ Редактирование сообщений (опционально)
   - ✅ Удаление сообщений (опционально)

4. **Подключите канал в системе**
   - Войдите в личный кабинет на https://content4u.ai
   - Перейдите в Settings → Social Media Accounts
   - Нажмите "Добавить Telegram канал"
   - Введите:
     * Название канала (для отображения)
     * Ссылку на канал: `https://t.me/mychannel` или `@mychannel`
   - Нажмите "Добавить"

5. **Проверьте подключение**
   - Система автоматически проверит что бот добавлен в канал
   - Если бот не админ - появится предупреждение
   - После исправления нажмите "Проверить" для повторной проверки

---

## 🔧 Использование в коде

### Публикация в канал пользователя

```python
from app.agents.publisher_agent import PublisherAgent
from app.models.content import ContentPiece, Platform

# Создаем агента
publisher = PublisherAgent()

# Создаем контент
content = ContentPiece(
    title="Заголовок поста",
    text="Текст поста для Telegram",
    platform=Platform.TELEGRAM,
    hashtags=["финансы", "инвестиции"]
)

# Публикуем в дефолтный канал пользователя
result = await publisher._publish_to_telegram(
    content=content,
    user_id=123  # ID пользователя
)

# Публикуем в конкретный канал
result = await publisher._publish_to_telegram(
    content=content,
    user_id=123,
    channel_id=456  # ID конкретного канала
)

if result.success:
    print(f"✅ Опубликовано! Post ID: {result.platform_post_id}")
    print(f"URL: {result.post_url}")
else:
    print(f"❌ Ошибка: {result.error_message}")
```

### Работа с сервисом каналов

```python
from app.services.telegram_channel_service import TelegramChannelService
from app.database.connection import get_db_session

db = next(get_db_session())
service = TelegramChannelService(db)

# Получить каналы пользователя
channels = service.get_user_channels(user_id=123)

for channel in channels:
    print(f"📱 {channel.channel_name}")
    print(f"   ✅ Verified: {channel.is_verified}")
    print(f"   📊 Posts: {channel.posts_count}")

# Получить дефолтный канал
default_channel = service.get_default_channel(user_id=123)

# Добавить канал (async)
import asyncio
success, message, channel = asyncio.run(
    service.add_channel(
        user_id=123,
        channel_link="https://t.me/mychannel",
        channel_name="Мой канал"
    )
)
```

---

## 🗄️ Структура БД

### Таблица `telegram_channels`

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Primary key |
| user_id | INTEGER | ID пользователя (FK → users.id) |
| channel_name | VARCHAR(255) | Название для UI |
| channel_username | VARCHAR(255) | @username если публичный |
| chat_id | VARCHAR(255) | ID канала в Telegram |
| is_active | BOOLEAN | Активен ли канал |
| is_verified | BOOLEAN | Бот - админ с правами |
| is_default | BOOLEAN | Канал по умолчанию |
| channel_title | VARCHAR(500) | Название из Telegram API |
| channel_type | VARCHAR(50) | channel/group/supergroup |
| members_count | INTEGER | Количество подписчиков |
| posts_count | INTEGER | Количество постов |
| last_post_at | DATETIME | Последняя публикация |
| last_error | TEXT | Последняя ошибка |
| created_at | DATETIME | Дата создания |
| updated_at | DATETIME | Дата обновления |

---

## 🔒 Безопасность

1. **Токен бота**
   - Храните только в `.env` (не в коде!)
   - Добавьте `.env` в `.gitignore`
   - Используйте GCP Secret Manager в продакшене

2. **Проверка прав**
   - Система автоматически проверяет что бот - администратор
   - Публикация возможна только в верифицированные каналы
   - Каждый пользователь видит только свои каналы

3. **Изоляция данных**
   - Пользователи изолированы на уровне БД (user_id)
   - API endpoints проверяют JWT токен
   - Невозможно получить доступ к чужим каналам

---

## 🧪 Тестирование

```bash
# Запуск всех тестов
python3 -m pytest tests/test_telegram_channels.py -v

# Тест конкретного функционала
python3 -m pytest tests/test_telegram_channels.py::test_add_channel -v

# С выводом логов
python3 -m pytest tests/test_telegram_channels.py -v -s
```

---

## ❓ FAQ

### Сколько каналов может подключить один пользователь?
Неограниченное количество.

### Нужно ли каждому пользователю создавать своего бота?
Нет! Используется один бот для всех.

### Как бот определяет в какой канал постить?
Через `chat_id` из таблицы `telegram_channels`.

### Что если пользователь удалит бота из канала?
При следующей публикации будет ошибка, которая сохранится в `last_error`.

### Как проверить что бот работает?
```bash
wget -q -O - "https://api.telegram.org/bot{TOKEN}/getMe"
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте что `TELEGRAM_BOT_TOKEN` установлен
2. Проверьте что бот - администратор канала
3. Проверьте логи: `tail -f logs/app.log`
4. Проверьте БД: `sqlite3 content_curator.db "SELECT * FROM telegram_channels;"`

---

**Дата создания:** 2025-10-20  
**Автор:** AI Content Orchestrator Team  
**Версия:** 1.0


