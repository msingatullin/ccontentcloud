# Multimedia Producer Agent - Техническое задание

## 📋 Обзор

**Multimedia Producer Agent** - специализированный агент для создания визуального контента в рамках системы Content Curator AI. Агент отвечает за генерацию изображений, создание инфографики, оптимизацию медиа-контента и подготовку визуальных материалов для различных платформ.

## 🎯 Функциональные требования

### Основные возможности:
- ✅ **Генерация изображений** на основе текстового описания
- ✅ **Создание инфографики** с использованием готовых шаблонов
- ✅ **Карусельные посты** для социальных сетей
- ✅ **Обложки для видео** и постов
- ✅ **Обработка и оптимизация** изображений под платформы
- ✅ **Интеграция с AI моделями** (DALL-E 3, Stable Diffusion)
- ✅ **Поиск стоковых изображений** через Unsplash API

### Специализированные функции:
- 🎨 **Система шаблонов** для инфографики
- ⚡ **Пакетная генерация** изображений
- 💾 **Кэширование** сгенерированного контента
- 🔧 **Автоматическая оптимизация** под платформы
- 📱 **Поддержка форматов** для всех основных соцсетей

## 🏗️ Технические требования

### Архитектура:
```python
class MultimediaProducerAgent(BaseAgent):
    """Агент для создания визуального контента"""
    
    # Capabilities
    task_types: [TaskType.PLANNED, TaskType.COMPLEX]
    max_concurrent_tasks: 2
    specializations: [
        "image_generation", 
        "infographics", 
        "visual_content", 
        "media_optimization"
    ]
    performance_score: 0.8  # Медленнее из-за обработки изображений
```

### MCP интеграции:
- **DALL-E 3 API** - основная модель генерации
- **Stable Diffusion API** - альтернативная модель
- **Unsplash API** - стоковые изображения
- **Image Optimization** - оптимизация медиа

## 📊 Поддерживаемые форматы

### Форматы изображений:
```python
class ImageFormat(Enum):
    SQUARE = "square"          # 1080x1080 (Instagram, Facebook)
    VERTICAL = "vertical"      # 1080x1350 (Instagram Stories, TikTok)
    HORIZONTAL = "horizontal"  # 1920x1080 (YouTube, Facebook Cover)
    CAROUSEL = "carousel"      # 1080x1080 (Instagram Carousel)
    BANNER = "banner"         # 1200x630 (Facebook, Twitter)
```

### Типы контента:
```python
class ContentType(Enum):
    IMAGE = "image"                    # Обычные изображения
    INFOGRAPHIC = "infographic"        # Инфографика
    CAROUSEL_POST = "carousel_post"    # Карусельные посты
    VIDEO_COVER = "video_cover"        # Обложки видео
    BANNER = "banner"                  # Баннеры
```

## 🎨 Система шаблонов

### Доступные шаблоны инфографики:

#### 1. Statistics Template
- **ID**: `stats_template`
- **Формат**: Square (1080x1080)
- **Назначение**: Отображение статистических данных
- **Элементы**: Заголовок, 2 статистики, описание, иконки

#### 2. Timeline Template
- **ID**: `timeline_template`
- **Формат**: Horizontal (1920x1080)
- **Назначение**: Временная шкала событий
- **Элементы**: Заголовок, 4 события, временная линия

## ⚙️ Конфигурация

### Настройки генерации:
```python
generation_settings = {
    "dalle": {
        "model": "dall-e-3",
        "quality": "standard",
        "size": "1024x1024",
        "style": "vivid"
    },
    "stable_diffusion": {
        "model": "stable-diffusion-xl-base-1.0",
        "steps": 20,
        "guidance_scale": 7.5
    }
}
```

### Настройки оптимизации:
```python
optimization_settings = {
    "web": {
        "max_width": 1920,
        "max_height": 1920,
        "quality": 85,
        "format": "jpeg"
    },
    "social": {
        "max_width": 1080,
        "max_height": 1080,
        "quality": 90,
        "format": "jpeg"
    }
}
```

## 🔄 Система кэширования

### Особенности:
- **TTL**: 24 часа для сгенерированных изображений
- **Формат**: JSON метаданные + PNG файлы
- **Расположение**: `cache/multimedia/`
- **Ключи**: MD5 хэш от prompt + format + style

### Структура кэша:
```
cache/multimedia/
├── {hash}.json      # Метаданные изображения
├── {hash}.png       # Файл изображения
└── ...
```

## 📱 Интеграция с платформами

### Поддерживаемые платформы:
- **Instagram** - Square, Vertical, Carousel
- **Facebook** - Square, Horizontal, Banner
- **YouTube** - Horizontal (обложки)
- **TikTok** - Vertical
- **Twitter** - Horizontal, Banner
- **LinkedIn** - Horizontal

### Автоматическая оптимизация:
- Изменение размера под формат платформы
- Сжатие для веб-оптимизации
- Конвертация в оптимальный формат
- Сохранение качества для печати

## 🚀 Примеры использования

### 1. Генерация изображения:
```python
task_data = {
    "prompt": "Modern office workspace with laptop and plants",
    "content_type": "image",
    "format": "square",
    "style": "professional"
}

result = await agent.execute_task(task)
```

### 2. Создание инфографики:
```python
task_data = {
    "content_type": "infographic",
    "template_id": "stats_template",
    "format": "square",
    "data": {
        "title": "AI Statistics 2024",
        "stat1": "85%",
        "stat2": "2.3M",
        "description": "Companies using AI"
    }
}
```

### 3. Пакетная генерация:
```python
requests = [
    ImageGenerationRequest(
        prompt="Professional headshot",
        content_type=ContentType.IMAGE,
        image_format=ImageFormat.SQUARE
    ),
    # ... больше запросов
]

results = await agent.create_image_batch(requests)
```

## 🔧 API Endpoints

### Основные методы:
- `execute_task(task)` - Выполнение задачи
- `generate_image(prompt, format, style)` - Генерация изображения
- `create_infographic(template_id, data)` - Создание инфографики
- `create_carousel_post(slides)` - Карусельный пост
- `create_video_cover(title, description)` - Обложка видео
- `optimize_image_for_platform(image_path, platform)` - Оптимизация
- `search_stock_images(query, count)` - Поиск стоковых изображений

### Вспомогательные методы:
- `get_available_templates()` - Список шаблонов
- `get_platform_formats()` - Форматы платформ
- `create_image_batch(requests)` - Пакетная генерация

## 📈 Производительность

### Метрики:
- **Время генерации**: 2-10 секунд (зависит от модели)
- **Размер файлов**: 100KB - 5MB
- **Поддерживаемые форматы**: PNG, JPEG, WebP
- **Максимальный размер**: 1920x1920 пикселей

### Ограничения:
- **Concurrent tasks**: 2 (из-за обработки больших файлов)
- **Performance score**: 0.8 (медленнее текстовых агентов)
- **Cache TTL**: 24 часа
- **API rate limits**: Зависят от провайдеров

## 🔐 Безопасность

### API ключи:
- `OPENAI_API_KEY` - для DALL-E 3
- `STABLE_DIFFUSION_API_KEY` - для Stable Diffusion
- `UNSPLASH_API_KEY` - для стоковых изображений

### Обработка ошибок:
- Fallback на заглушки при недоступности API
- Graceful degradation при ошибках
- Логирование всех операций
- Валидация входных данных

## 🧪 Тестирование

### Запуск примеров:
```bash
cd content-curator-ai/app/agents/
python multimedia_example.py
```

### Тестовые сценарии:
- ✅ Генерация одиночного изображения
- ✅ Создание инфографики
- ✅ Карусельный пост
- ✅ Обложка видео
- ✅ Пакетная генерация
- ✅ Поиск стоковых изображений
- ✅ Оптимизация изображений

## 📝 Логирование

### Уровни логирования:
- **INFO**: Успешные операции, кэш попадания
- **WARNING**: Fallback на заглушки, устаревший кэш
- **ERROR**: Ошибки API, проблемы с файлами

### Структура логов:
```
[INFO] MultimediaProducerAgent multimedia_producer_agent инициализирован
[INFO] Генерация изображения DALL-E: Modern office workspace...
[INFO] Изображение сгенерировано за 3.45с: cache/multimedia/abc123.png
[INFO] Изображение оптимизировано для instagram: image_optimized_instagram.jpg
```

## 🔮 Планы развития

### Ближайшие улучшения:
- [ ] Интеграция с Midjourney API
- [ ] Поддержка видео генерации
- [ ] Расширенная система шаблонов
- [ ] AI-анализ качества изображений
- [ ] Автоматическое A/B тестирование

### Долгосрочные цели:
- [ ] Собственная модель генерации
- [ ] Интеграция с 3D моделями
- [ ] Поддержка анимации
- [ ] Реальное время генерации
- [ ] Персонализация под бренд

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи агента
2. Убедитесь в корректности API ключей
3. Проверьте доступность внешних сервисов
4. Обратитесь к примерам использования

**Multimedia Producer Agent** готов к использованию в продакшене! 🚀
